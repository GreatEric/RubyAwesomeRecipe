<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Awesome Recipe", root_path, class: "navbar-brand", id: "logo" %>
      <%if logged_in? %>
          <%= link_to "Hi, #{current_user.name}", user_path(current_user) , class: 'btn btn-primary btn-warning logbtn'%>
          <%= link_to "Add Recipe", new_recipe_path , class: 'btn btn-primary btn-success logbtn'%>
      <%end%>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav" id="sort-recipes-by">
      <li><%= link_to "All Recipes From New To Old", recipes_sorted_by_newest_path %></li>
      <li><%= link_to "Sort by Fastest Recipes", recipes_sorted_by_cook_time_path %></li>
      <li><%= link_to "All Recipes From Most Popular", recipes_sorted_by_favorite_path %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
          <%if logged_in? %>
            <%= link_to "Sign out", logout_path, method: :delete , class: 'btn btn-primary btn-danger logbtn'%>
          <% else %>
          <%= link_to "Sign in", login_path , class: 'btn btn-primary btn-large logbtn'%>
          <%= link_to "Sign up", signup_path , class: 'btn btn-primary btn-large logbtn' %>
         <%end%>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>